A full-stack application built with Next.js 15,integrated with Prisma and MongoDB. It includes the ability to add, delete, and edit blog posts, data fetching (server-side) and performing operations with REST API. Form management is handled using React Hook Form, and TypeScript is used for type safety across the applicationt.
this app will have ability of filtring blogs and authentication with nextauth.

